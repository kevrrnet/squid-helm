# # templates/pvc.yaml
# apiVersion: v1
# kind: PersistentVolumeClaim
# metadata:
#   name: "{{ .Release.Name }}-pvc"
# spec:
#   accessModes:
#     - ReadWriteOnce
#   storageClassName: fast
#   resources:
#     requests:
#       storage: {{ .Values.persistence.size }}